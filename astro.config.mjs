import { defineConfig } from 'astro/config';
import github from '@astrojs/github';
// https://astro.build/config
export default defineConfig({  site: 'https://rockvns.github.io',
  base: 'my-repo',});
